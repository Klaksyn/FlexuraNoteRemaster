# How to include styles in my program (e.g. qss files)

In src/helpers contains style_loader where has func of 
QString loadStyleFile(const QString& pathStyle)

this func is using in main.cpp and add in variable of
combinedStyle to include all styles which you choiced

